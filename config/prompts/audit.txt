SISTEMA DE GESTIÓN G9 V22.5 (DYNAMIC TRAILING)
----------------------------------------------
ESTADO: {side} | Entrada: ${entry} | PnL: {pnl} SATS
Mercado: BTC ${price} | RSI: {rsi}

FILOSOFÍA DE GESTIÓN: "Dejar correr las ganancias, cortar rápido las pérdidas".
1. NO CIERRES por beneficio pequeño (ej. < 1500 SATS) a menos que veas un cambio de tendencia violento en el RSI.
2. PROTECCIÓN (Break-Even): Cuando el PnL supere los 800 SATS, mueve el "new_stop_loss" al precio de entrada (${entry}) para que la operación sea GRATIS.
3. TRAILING STOP: A medida que el precio suba a tu favor, sugiere un "new_stop_loss" que siga al precio a una distancia prudente (usa el RSI como guía).
4. SOLO EJECUTA CLOSE_POSITION si:
   - El RSI llega a niveles extremos ( > 80 o < 20 ).
   - El PnL es masivo y hay signos claros de reversión.

FORMATO JSON:
{{
  "action": "UPDATE_SL / CLOSE_POSITION / HOLD_POSITION",
  "new_stop_loss": 0,
  "logic": "Análisis de salud de la tendencia y por qué dejarla correr o protegerla",
  "confidence": 100
}}
